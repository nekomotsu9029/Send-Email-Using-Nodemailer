# Install dependencies
_Open a terminal in the project root folder_
    npm install

## Run the server
    npm run dev
    npm run start
_(dev) to run the project with nodemon_
_(start) to run with nodemon_

### End points
_('/api/sendEmail') input: {email, subject, body} / output: {state, message}_